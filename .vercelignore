# Build output (built on Vercel servers, not needed in upload)
.next
out

# Dependencies (installed on Vercel servers)
node_modules

# Test artifacts
test-results
playwright-report
playwright/.cache

# Dev-only tooling
e2e
docs

# Local env files (Vercel uses its own env vars)
.env
.env*.local
.env.production.local
